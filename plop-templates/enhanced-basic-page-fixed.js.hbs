{{#if section}}
'use client';
{{/if}}

/**
 * @file {{proper name}} page
 * @module app/{{section}}/{{name}}/page
 */

import React from 'react';
import { Box, Typography, Container, Paper } from '@mui/material';

/**
 * Metadata for {{name}} page
 * @type {Object}
 */
export const metadata = {
  title: "{{title}}",
  description: "{{title}} page",
};

/**
 * {{proper name}} page component
 * 
 * @component
 * @returns {React.ReactElement} {{proper name}} page component
 */
export default function {{pascalCase name}}Page() {
  return (
    <Container maxWidth="lg">
      <Box sx=\{\{ py: 4 \}\}>
        <Typography variant="h4" component="h1" gutterBottom>
          {{proper name}}
        </Typography>
        
        <Paper sx=\{\{ p: 3 \}\}>
          <Typography paragraph>
            This is the {{name}} page. Customize this content to suit your needs.
          </Typography>
        </Paper>
      </Box>
    </Container>
  );
  
}
