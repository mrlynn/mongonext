'use client';

import React from 'react';
import { Box } from '@mui/material';

const {{name}} = ({{{{raw}}}}{ children }{{{{/raw}}}}) => {
  {{{{raw}}}}
  return (
    <Box>
      {children}
    </Box>
  );
  {{{{/raw}}}}
};

export default {{name}};
