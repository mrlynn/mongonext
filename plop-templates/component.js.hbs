'use client';
 
import React from 'react';
import { Box } from '@mui/material';

const [[name]] = ({ children }) => {
  return (
    <Box>
      {children}
    </Box>
  );
};

export default [[name]];
