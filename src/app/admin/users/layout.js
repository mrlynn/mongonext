export const metadata = {
  title: "User Management | MongoNext Admin",
  description: "Manage users, roles, and permissions",
};

export default function UsersLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
} 